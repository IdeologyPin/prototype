
assets
-------
manually managed js libraries/packages and css and other assets.

node_modules <- Package.json
-------------------------------
all other JS libraries are managed by Node package manager(npm).
Packages required for angular js based web client.
#install using node. (node is used just to get packages, may use as a build tool)
#for ease of deployment and code management backend and frontend both run on flask in our prototype. no node frontend server
